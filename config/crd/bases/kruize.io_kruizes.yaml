---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: kruizes.kruize.io
spec:
  group: kruize.io
  names:
    kind: Kruize
    listKind: KruizeList
    plural: kruizes
    singular: kruize
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Kruize contains configuration options for controlling the deployment of the Kruize
          application and its related components. A Kruize instance must be created to instruct
          the operator to deploy the Kruize application.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KruizeSpec defines the desired state of Kruize
            properties:
              autotune_image:
                description: Container image for Kruize Autotune
                type: string
              autotune_ui_image:
                description: Container image for Kruize UI
                type: string
              cluster_type:
                description: Type of Kubernetes cluster (openshift, minikube, or kind)
                type: string
              namespace:
                description: Target namespace for Kruize deployment
                type: string
              resources:
                description: Resource configuration for Kruize components
                properties:
                  database:
                    description: Database resource configuration
                    properties:
                      cpuLimit:
                        description: CPU limit (e.g., "0.5", "500m")
                        type: string
                      cpuRequest:
                        description: CPU request (e.g., "0.5", "500m")
                        type: string
                      memoryLimit:
                        description: Memory limit (e.g., "100Mi", "1Gi")
                        type: string
                      memoryRequest:
                        description: Memory request (e.g., "100Mi", "1Gi")
                        type: string
                    type: object
                  kruize:
                    description: Kruize application resource configuration
                    properties:
                      cpuLimit:
                        description: CPU limit (e.g., "0.5", "500m")
                        type: string
                      cpuRequest:
                        description: CPU request (e.g., "0.5", "500m")
                        type: string
                      memoryLimit:
                        description: Memory limit (e.g., "100Mi", "1Gi")
                        type: string
                      memoryRequest:
                        description: Memory request (e.g., "100Mi", "1Gi")
                        type: string
                    type: object
                  persistentVolume:
                    description: Persistent Volume configuration
                    properties:
                      accessModes:
                        description: |-
                          Access modes for the persistent volume
                          Valid values: ReadWriteOnce, ReadOnlyMany, ReadWriteMany, ReadWriteOncePod
                        items:
                          description: PersistentVolumeAccessMode defines the access
                            mode for persistent volumes
                          enum:
                          - ReadWriteOnce
                          - ReadOnlyMany
                          - ReadWriteMany
                          - ReadWriteOncePod
                          type: string
                        maxItems: 4
                        type: array
                      hostPath:
                        description: Host path for the persistent volume
                        type: string
                      pvStorageSize:
                        description: |-
                          Storage size for PersistentVolume (e.g., "500Mi", "1Gi")
                          If not specified, defaults to 500Mi for OpenShift, 1Gi for Kubernetes
                        type: string
                      pvcStorageSize:
                        description: |-
                          Storage size for PersistentVolumeClaim (e.g., "500Mi", "1Gi")
                          If not specified, uses PVStorageSize value
                        type: string
                      storageClassName:
                        description: Storage class name
                        type: string
                    type: object
                type: object
            required:
            - autotune_image
            - autotune_ui_image
            - cluster_type
            - namespace
            type: object
          status:
            description: KruizeStatus defines the observed state of Kruize
            properties:
              nodes:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  type: string
                type: array
            required:
            - nodes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
